import styled from "styled-components";

export const PageWrap = styled.div`
  max-width: 500px;
  width: 100%;
  margin: 0 auto;
  padding: 0 32px;
`;

const Layout = {
  PageWrap,
};

export default Layout;
